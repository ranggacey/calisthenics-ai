# Calisthenics AI Evolution Log 🧬

Auto-generated by self-evolve cronjob.

---

## [2026-08-02 20:15] Fix 3 Bug Kualitas: Durasi Double-Count saat Pause, Feedback Arah Terbalik (pullup/crunch), Plank Silent Bad-Form
- **BUG #1 — durasi session double-count kalau Finish & Save ditekan saat PAUSE:** `finishSession` hitung `now − start + pausedAccum` — kalau session lagi paused, `now − start` MASIH mencakup durasi pause (sessionStartRef tidak di-reset saat pause, cuma saat resume) → pause dihitung 2x (mis. latihan 2 menit + pause 5 menit → durasi tersimpan 7 menit). Fix: kalau `state.isPaused`, base durasi = `pausedAccumRef.current` saja (akumulasi pause sudah tersimpan saat togglePause); kalau aktif, `now − start + pausedAccum`. Durasi di-floor ke minimal 1 detik.
- **BUG #2 — feedback form TERBALIK untuk exercise inverted (pullup/crunch):** `getFormFeedback` cek kedalaman pakai `angle > target+tol` (asumsi down = angle kecil) dan ekstensi pakai `angle < upAngle−15` — untuk pullup/crunch (inverted, down = angle BESAR) kondisi jadi kebalikan: user yang belom turun penuh (angle < 160) malah dianggap "sudah dalam" dan diam, user yang hyperextend malah kena warning "Turun lebih dalam!". Fix: kondisi jadi arah-aware — inverted: shallow = `angle < target−tol` ("Turun perlahan sampai lurus penuh!"), partial = `angle > upAngle+15` ("Angkat penuh sampai atas!").
- **BUG #3 — plank silent bad-form:** branch form jelek plank (`else`) cuma setState kalau `plankStart !== 0` — user yang MULAI dengan pinggul drop (belum pernah latch posisi baik) tidak dapat feedback apa pun: "Form: Good" tetap tampil, tanpa beep. Fix: setState formGood:false + pesan juga di cabang `plankStart === 0`; gate beep `wasFormBad` tetap di luar (edge-triggered, anti-spam).
- Verifikasi: eslint exit 0, `npm run build` SUCCESS (8 route static).
- File yang diubah: `src/components/PoseDetector.tsx`

## [2026-08-02 16:20] Fix metric plank di Daily Quests (reps → seconds)
- **BUG KONSISTENSI:** quest "Plank Time" dilabel `metric: "reps"` dengan goal 60, padahal PoseDetector mode `isHold` menyimpan DETIK (bukan rep) ke storage. Quest menuntut "60 reps" tapi data log adalah 60 detik → progress plank selalu tampil jauh dari target & label salah.
- **FIX:** `metric` QuestDef diperluas ke union `"reps" | "workouts" | "seconds"`; quest plank diubah ke `metric: "seconds"` dengan deskripsi "Hold plank for 60 seconds today". Tampilan progress sudah benar karena `progressFor` membaca `log.byExercise[exercise]` apa adanya (nilai detik).
- Alignment dengan dashboard (label "Plank sec" sudah benar).
- File yang diubah: `src/components/DailyQuests.tsx`

## [2026-08-02 18:00] Konsolidasi localStorage ke storage.ts (WeeklyActivity, WorkoutHistory, Achievements)
- **Refactor konsistensi arsitektur:** 3 komponen data mem-bypass storage.ts dan akses `localStorage.getItem/setItem` langsung + hardcode key string (`cali_history`, `cali_achievements`). Ini melanggar aturan proyek "SEMUA localStorage lewat storage.ts". Diarahkan ulang:
  - `WeeklyActivity.tsx` + `WorkoutHistory.tsx`: baca history via `loadHistory()` (hapus interface `SessionRecord` duplikat + const KEY lokal), baca `type SessionRecord` dari storage.ts.
  - `Achievements.tsx`: simpan achievements via `loadAchievements()`/`saveAchievements()` (gantikan `localStorage.getItem/setItem("cali_achievements")` — sekaligus ikut dispatch event `storage` via writeJson, konsisten dengan widget lain).
- **Manfaat:** satu sumber kebenaran untuk key localStorage (mudah migrasi key ke depan), komponen lebih ramping, dan penulisan lewat `writeJson` memastikan same-tab live refresh konsisten.
- Tidak mengubah logic/visual — murni konsolidasi arsitektur + type safety.
- Build SUCCESS, eslint clean (exit 0).
- File yang diubah: `src/components/WeeklyActivity.tsx`, `src/components/WorkoutHistory.tsx`, `src/components/Achievements.tsx`

## [2026-08-02 10:00] Fix Anti Double-Count Rep + Anti-Spam Beep + Bersihkan Repo
- **BUG FIX — double-count rep:** state machine sebelumnya bisa menghitung rep ganda saat user goyang kecil di posisi bawah (tanpa kembali penuh ke posisi atas). Ditambah flag `sawUp` — transisi "down" hanya valid jika user benar-benar di posisi atas (konfirmasi `upFrames`), dan di-reset saat rep selesai. Rep sekarang butuh siklus penuh up→down→up.
- **BUG FIX — spam beep saat rep gagal:** `playFormBeep()` di cabang rep-gagal dipanggil tanpa gate → bunyi/getar berulang tiap rep gagal. Sekarang pakai gate `wasFormBad` (beep hanya saat transisi ke form jelek).
- **Housekeeping:** hapus file junk yang ter-track (`c.txt`, `build_log.txt`, `evolution-worker.ts` — tidak direferensikan) dan sub-proyek `video-gen-ai/` (percobaan Remotion terpisah, 563MB node_modules, lockfile-nya bikin warning "inferred workspace root").
- File yang diubah: `src/components/PoseDetector.tsx`; dihapus: `c.txt`, `build_log.txt`, `evolution-worker.ts`, `video-gen-ai/`

## [2026-08-02 08:30] Optimalisasi Feedback Form
- **Optimalisasi Feedback Form:** Menyederhanakan logika `badFormReason` menjadi `getFormFeedback` untuk memberikan feedback yang lebih prioritas dan tidak membingungkan pengguna. Pesan feedback sekarang lebih relevan dengan kondisi form (bilateral, straightness, kedalaman) dan mencegah spam.
- **Pesan Lebih Jelas:** Mengganti "Nice! Keep going 💪" menjadi "Keren! Lanjut terus 💪" agar lebih sesuai dengan bahasa Indonesia.
- File yang diubah: `src/components/PoseDetector.tsx`

## [2026-08-02 06:00] Fix Spam Feedback & Reset Counter Saat Ganti Exercise
- **BUG FIX — beep/vibrate spam:** `playFormBeep()` sebelumnya dipanggil SETIAP frame (~30x/detik) selama form jelek, bikin getar + bunyi spam. Sekarang pakai gate `wasFormBad` — beep/vibrate hanya sekali saat transisi ke form jelek, reset saat form kembali baik.
- **BUG FIX — counter bocor antar exercise:** ganti exercise (mis. 10 squat → pushup) sebelumnya TIDAK reset `state.repCount`/`sessionRepsRef` → rep exercise lama ikut tersimpan di session exercise baru (data salah). Sekarang reset 1x per ganti exercise via flag `needsSessionReset`, termasuk reset `sessionStartRef` supaya durasi summary akurat (hanya waktu exercise aktif).
- **Refactor:** `togglePause` & `resetSession` dibungkus `useCallback` (stabil, bersih).
- File yang diubah: `src/components/PoseDetector.tsx`

## [2026-08-01 12:00] Implementasi Feedback Form Lebih Spesifik
- Menambahkan fungsi `badFormReason` di `src/components/PoseDetector.tsx` untuk memberikan feedback yang lebih jelas kepada pengguna mengenai kesalahan form saat latihan.
- Feedback mencakup deteksi badan tidak lurus (hip drop), ketidakseimbangan bilateral, serta kedalaman gerakan.
- File yang diubah: `src/components/PoseDetector.tsx`

## [2026-08-01 15:02] Tambahkan Fungsi badFormReason di PoseDetector
- Menambahkan fungsi  di  untuk memberikan feedback form yang lebih spesifik (badan tidak lurus, bilateral, angle gerakan).
- File yang diubah: 

## [2026-08-01 15:02] Tambahkan Fungsi badFormReason di PoseDetector
- Menambahkan fungsi `badFormReason` di `src/components/PoseDetector.tsx` untuk memberikan feedback form yang lebih spesifik (badan tidak lurus, bilateral, angle gerakan).
- File yang diubah: `src/components/PoseDetector.tsx`

## [2026-08-01 15:04] Perbaiki Duplikasi dan Posisi badFormReason
- Memperbaiki error build dengan menghapus duplikasi definisi fungsi `badFormReason` dan memindahkannya ke posisi yang benar di `src/components/PoseDetector.tsx`.
- File yang diubah: `src/components/PoseDetector.tsx`

## [2026-08-01 15:05] Perbaikan Logika Rep Counting dan Form Feedback
- Memastikan rep hanya terhitung jika form (bilateral dan straightness) bagus.
- Mengintegrasikan fungsi `badFormReason` untuk pesan form yang lebih deskriptif.
- Menyesuaikan state `formGood` agar selalu akurat dengan kondisi form terkini.
- File yang diubah: `src/components/PoseDetector.tsx`

## [2026-08-01 15:06] Perbaikan Linting Error dan Type Safety
- **`PoseDetector.tsx`:** Memperbaiki error `stateRef.current = state;` dan `useRef(Date.now());`. Mengganti tipe `any` dengan tipe yang lebih spesifik (`import("@mediapipe/pose").Results`) atau `unknown` dengan *type narrowing* yang tepat. Menambahkan null check untuk `pose.send()`.
- **`layout.tsx`:** Mengganti semua tag `<a>` dengan komponen `<Link />` dari Next.js untuk navigasi internal.
- **`Achievements.tsx`:** Memperbaiki error `setState` di dalam `useEffect` dengan memindahkan logika update ke fungsi terpisah yang dipanggil secara kondisional.
- File yang diubah: `src/components/PoseDetector.tsx`, `src/app/layout.tsx`, `src/components/Achievements.tsx`

## [2026-08-02 00:30] Perbaikan Kritikal Rep Counting + 5 Exercise Baru + UX Upgrade
- **BUG KRITIKAL FIX:** Logika `angleOk` memeriksa sudut ≈ `targetAngle` (90°) PADA SAAT rep selesai (posisi atas ~160°), sehingga rep TIDAK PERNAH terhitung. Diganti dengan `reachedBottom` flag — rep hanya dihitung jika user benar-benar mencapai posisi bawah yang benar selama fase "down".
- **badFormReason dinamis:** feedback sekarang kontekstual per fase (up/down), pesan "Extend fully at the top!" saat fase up, "Go deeper!" saat fase down.
- **5 exercise baru di PoseDetector:** pullup, dips, lunges, crunch, burpee (semua pakai landmark MediaPipe yang tepat).
- **ExerciseSelector:** 8 exercise total, grid 4 kolom.
- **DailyQuests:** quest baru untuk pullup, dips, lunges, crunch, burpee (total 9 quest).
- **Dashboard:** panel "Today" sekarang menampilkan semua 9 exercise.
- **Haptic feedback:** Vibration API (navigator.vibrate) untuk rep count, form warning, phase transition. Toggle baru `HapticToggle` (localStorage `cali_haptic`).
- **Audio enhanced:** rep beep sekarang sine wave 880Hz, form warning sawtooth 220Hz, phase transition triangle 440Hz.
- **Rest Timer:** komponen baru dengan preset 30s/60s/90s/2m/3m, progress bar, sound + vibration saat selesai, persist pilihan di localStorage (`cali_rest_timer`).
- **Workout Summary Modal:** setelah "Finish & Save", muncul modal statistik sesi (reps, durasi, reps/min).
- File yang diubah: `src/components/PoseDetector.tsx`, `src/components/ExerciseSelector.tsx`, `src/components/DailyQuests.tsx`, `src/components/RestTimer.tsx` (baru), `src/components/HapticToggle.tsx` (baru), `src/app/dashboard/page.tsx`, `src/app/workout/page.tsx`

## [2026-08-02 14:30] Fix Timezone UTC→Lokal, Live Refresh, Anti-Spam Plank Beep, Cleanup
- **BUG TIMEZONE (kritis untuk WIB):** semua tanggal harian (daily quests reset, streak, weekly activity) pakai `toISOString().slice(0,10)` = UTC. Di WIB quest reset jam 07:00 pagi, bukan tengah malam. Fix: helper `localDateKey()` di storage.ts (pakai getFullYear/getMonth/getDate lokal) — dipakai di saveWorkoutSession (streak), todayKey (daily log), WeeklyActivity (bucket 7 hari).
- **LIVE REFRESH tab sama:** `writeJson()` sekarang dispatch `window.dispatchEvent(new Event("storage"))` — komponen di tab yang sama (StatsDashboard, DailyQuests, ChallengeTracker, Achievements, WorkoutHistory, WeeklyActivity, HomeChallenge) langsung update setelah "Finish & Save", tanpa reload manual. Storage event asli cuma fire antar-tab; handler komponen hanya baca localStorage → tanpa loop.
- **ANTI-SPAM plank beep:** beep form warning di mode plank (isHold) tadinya tanpa gate `wasFormBad` — potensi bunyi tiap frame selama pinggul drop. Sekarang edge-triggered (beep sekali saat transisi ke form jelek, gate di-reset saat form baik).
- **Dead code:** hapus `logSession` duplikat (tak pernah dipakai) di WorkoutHistory.tsx — logSession resmi dari storage.ts.
- **Listener leak:** Achievements.tsx useEffect dep `[stats]` → `[]` (listener di-register ulang tiap statistik berubah). Sekarang baca stats fresh dari storage tiap event.
- **Durasi di history:** WorkoutHistory menampilkan durasi tiap sesi (mm:ss) + label hari WeeklyActivity pakai tanggal lokal.
- **Lokalisasi:** pesan plank "Keep it straight!" → "Pertahankan!", "Hips dropping!" → "Pinggul turun! Jaga badan tetap lurus".
- File yang diubah: `src/lib/storage.ts`, `src/components/WeeklyActivity.tsx`, `src/components/WorkoutHistory.tsx`, `src/components/Achievements.tsx`, `src/components/PoseDetector.tsx`

## [2026-08-02 19:10] Fix Kritikal Rep Counting Pull-Up & Crunch + Overshoot Latch + Durasi Pause
- **BUG KRITIKAL #1 — rep pull-up & crunch TIDAK PERNAH terhitung:** `targetAngle` kedua exercise = 90°, padahal posisi bawah = 160° (pull-up: hanging dengan elbow lurus; crunch: berbaring dengan torso lurus). Latch `reachedBottom` cek `|angle − 90| ≤ 25` → di posisi bawah angle ~160°, jauh dari range → latch mati → rep selalu ditolak. Fix: `targetAngle` pull-up & crunch → 160 (sama dengan `downAngle`), supaya latch cocok dengan posisi bawah asli. (Simulasi node: 6/8 exercise latch MATI sebelum fix, semua HIDUP setelah.)
- **BUG KRITIKAL #2 — arah gerakan inverted:** pull-up & crunch adalah exercise dengan arah angle TERBALIK — saat turun (down), angle elbow/torso NAIK dari ~60° (tekuk) ke ~160° (lurus/hanging). State machine lama mengasumsikan down = angle turun (`isDown = angle <= downAngle+10` = selalu true untuk pull-up yang angle-nya di atas 150° → transisi up tidak pernah terjadi). Fix: flag baru `inverted?: boolean` di interface Exercise; `isDown`/`isUp`/`passedTarget` sekarang hormati arah — inverted: down = angle ≥ downAngle−10, up = angle ≤ upAngle+10.
- **BUG KRITIKAL #3 — straightTriplet crunch salah:** `[11,23,25]` (shoulder-hip-knee) — saat crunch-up torso 60° → deviasi dari 180° = 120° → `straightOk` mati → rep gagal walau form benar. Fix: `[23,11,12]` (hip-shoulder-shoulder) — garis badan yang tidak berubah saat crunch, konsisten dengan pushup/plank.
- **ROBUSTNESS — overshoot latch:** `reachedBottom` sebelumnya hanya latch saat angle PAS di dalam target range (`|angle−target| ≤ tol`). Squat dalam (angle < 70°) tidak pernah latch → rep ditolak walau gerakan penuh. Fix: latch juga saat angle sudah LEWAT target range (`passedTarget`), arah gerakan (`isDown`) tetap divalidasi. Normal: `angle < target − tol`; inverted: `angle > target + tol`.
- **QUALITY — durasi session akurat:** pause tidak lagi dihitung sebagai waktu latihan. `pausedAccumRef` menyimpan akumulasi detik aktif sebelum pause; `sessionStartRef` di-reset saat resume; `finishSession` hitung `(now − start + pausedAccum)`. Reset saat ganti exercise & tombol Reset.
- Verifikasi: eslint exit 0, `npm run build` SUCCESS (8 route static).
- File yang diubah: `src/components/PoseDetector.tsx`

## [2026-08-02 21:05] Fix Unit Hold Exercise (plank) di History/Dashboard + Icon Haptic + Leaderboard Broken Classes
- **BUG - WorkoutHistory & Dashboard Recent Sessions tampilkan "60 reps" untuk plank:** hold exercise (plank) menyimpan DETIK ke session record, tapi history menampilkan reps tanpa label unit, plank 60 detik tampak "60 reps" (unit salah). Fix: getExerciseById(r.exercise).isHold menjadi label sec di WorkoutHistory; dashboard pakai r.exercise === "plank" ? "sec" : "reps".
- **BUG - HapticToggle icon sama saat on/off:** emoji hardcoded dua-duanya, off state tidak terlihat bedanya. Fix: lucide Vibrate (on) / VibrateOff (off), konsisten policy icons lucide-react (verifikasi node: dua-duanya ADA di 1.28.0).
- **BUG - Leaderboard pakai class shadcn yang TIDAK ADA di globals.css:** bg-card, text-primary, text-muted-foreground, row tampil transparan/broken (class undefined = tanpa styling). Fix: konsolidasi ke slate palette konsisten (bg-slate-950 page, bg-slate-900 border-slate-700 row, text-emerald-400 pts) nyambung dengan halaman lain.
- Verifikasi: eslint exit 0, npm run build SUCCESS (8 route static).
- File yang diubah: src/components/WorkoutHistory.tsx, src/components/HapticToggle.tsx, src/app/leaderboard/page.tsx, src/app/dashboard/page.tsx

## [2026-08-02 22:00] Perbaikan Akurasi Rep Counting (Lunges/Crunch Bilateral, Rep Cepat)
- **BUG KRITIKAL — Rep Lunges & Crunch tidak terhitung karena bilateral check:** Lunges secara desain asimetris (kaki depan tekuk, kaki belakang lurus → selisih angle bilateral besar), crunch gerakannya di torso (bukan lengan) → bilateral check selalu `false` → rep tidak pernah terhitung.
- **FIX #1 (Bilateral):** Menambah properti `checkBilateral?: boolean` (default `true`) di interface `Exercise`. Set `checkBilateral: false` untuk `lunges` dan `crunch`. Logika `bilateralOk` di `PoseDetector.tsx` sekarang hanya dijalankan jika `exercise.checkBilateral` tidak `false`.
- **BUG — Rep cepat tidak terhitung:** Pengguna dengan tempo latihan cepat tidak terhitung rep-nya karena `REQUIRED_FRAMES` (konfirmasi frame) di zona ekstrem (angle tertekuk penuh/lurus penuh) terlalu sulit dicapai dalam waktu singkat.
- **FIX #2 (Rep Cepat):** Mengubah mekanisme konfirmasi frame dari zona ekstrem ke "zona perjalanan" (transisi).
  - Mengganti `REQUIRED_FRAMES` menjadi `REQUIRED_DOWN_FRAMES = 3` (untuk transisi turun) dan `REQUIRED_UP_FRAMES = 2` (untuk transisi naik). Ini memberikan konfirmasi kuat saat turun (anti-phantom) dan responsif saat naik (rep cepat).
  - Zona perjalanan: `headingDown` (angle < `upAngle - 20`) dan `headingUp` (angle > `downAngle + 20`) memastikan zona ini tidak overlap dan mencakup sebagian besar gerakan transisi.
  - `REP_COOLDOWN_MS` diperpendek ke `400ms` agar rep cepat (tempo 1s/rep) terhitung.
- **Housekeeping:** Menghapus deklarasi `isUp` yang tidak terpakai dan komentar debug lama.
- **Verifikasi:** Simulasi di Node.js (`sim7-final-v3.mjs`) menunjukkan semua 8 exercise (termasuk lunges, crunch, pullup) terhitung 3/3 rep dalam tempo normal dan cepat (1s/rep), serta lulus uji anti-phantom. Build SUCCESS, eslint clean (exit 0).
- File yang diubah: `src/components/PoseDetector.tsx`

## [2026-08-02 23:00] Perbaikan Definisi Burpee
- **ANOMALI DEFINISI:** `keypoints` Burpee sebelumnya (`[11, 13, 15]` = shoulder-elbow-wrist) fokus pada gerakan push-up, padahal `upAngle` Burpee seharusnya adalah berdiri tegak/melompat. Ini membuat rep counting Burpee kurang akurat untuk siklus gerakan penuh.
- **FIX:** Mengubah `keypoints` Burpee menjadi `[23, 25, 27]` (hip-knee-ankle) untuk lebih akurat menangkap fase berdiri/squat. `upAngle` disesuaikan menjadi 170 derajat untuk posisi berdiri tegak. Properti `leftTriplet` dan `rightTriplet` dihapus dari definisi Burpee karena tidak relevan lagi setelah perubahan `keypoints` ini.
- **Type Safety:** Menambahkan `?` pada `leftTriplet` dan `rightTriplet` di interface `Exercise` agar bisa opsional, serta menambahkan null check saat menggunakan `exercise.leftTriplet` dan `exercise.rightTriplet`.
- **Verifikasi:** `npm run build` SUCCESS.
- File yang diubah: `src/components/PoseDetector.tsx`
## [2026-08-03 00:23] Fix: rename duration param in saveWorkoutSession - Updated function signature and usage.
